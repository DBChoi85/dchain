# dchain

## 환경 구성
python 3.11 이상 필요

Dependency install
```
pip install -r requirements.txt
```
접속 port 변경 시 run.py 수정
```
   if __name__ == '__main__':
       app.run(host='0.0.0.0', port=5000, debug=True) #port 수정
```

## 실행
```
python run.py
```



## 각종 DB 및 DID 생성 위치.
